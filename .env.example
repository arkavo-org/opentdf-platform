# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# API key for OpenRouter, used to reach the TypeSafe Jev decision model.
# Create one at https://openrouter.ai/keys
# Only needed to run the live Jev tests (-tags jevlive) or to enable a Jev seam.
OPENROUTER_API_KEY=
